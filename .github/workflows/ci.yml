name: Build Website

on:
  push:
  pull_request:

jobs:
  build:
    uses: bash/satellite/.github/workflows/build-reusable.yml@main
    with:
      workflow-ref: main
    permissions:
      pages: write
      id-token: write
